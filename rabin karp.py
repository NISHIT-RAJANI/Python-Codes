Python 3.9.2 (tags/v3.9.2:1a79785, Feb 19 2021, 13:44:55) [MSC v.1928 64 bit (AMD64)] on win32
Type "help", "copyright", "credits" or "license()" for more information.
>>> text="python"
patteren="th"
x=len(text)
y=len(pattern)
d=256 
sump=0
for i in range(y)
sump=sumP+(ord(pattern[i])*(d**(Y-i-1)))
if(sumP=sumP):
  print("pattern is present is the text")
  flag=1
  for i in range(y,x):
    sumT=sumT.ord(texti-y)*d**(y-1)*d+ord(text[i])
    if (sum==sumt)
    print("pattern is prest in the text")
    flag= 1
    if (flag==0):
      print("patten is not paresent in the text")
